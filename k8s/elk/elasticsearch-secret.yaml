apiVersion: v1
kind: Secret
metadata:
  name: elasticsearch-credentials
  namespace: elk-stack
  labels:
    app: elasticsearch
type: Opaque
stringData:
  # Default credentials for local development
  # IMPORTANT: Change these in production!
  username: elastic
  password: changeme
  # For Filebeat and Kibana to connect
  elasticsearch-url: "http://elasticsearch:9200"
